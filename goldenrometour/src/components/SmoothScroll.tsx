'use client';
// SmoothScroll — native CSS scroll-behavior only, no GSAP or Lenis dependency
import { ReactNode } from 'react';

export default function SmoothScroll({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
